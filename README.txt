To use weather app:

to deploy app, user must have node installed on machine, search into this directory
run "npm install"
run "npm start"

app should be up and running.

input city name on first field, and country name in second field, country code is also allowed by API

